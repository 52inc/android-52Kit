<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ftinc.kit">

    <application>

        <!-- Activity definition for Attributr -->
        <activity android:name=".ui.attributr.ui.LicenseActivity"
            android:label="@string/activity_license_label" />

        <!-- The detail activity for Attributr -->
        <activity android:name=".ui.attributr.ui.DetailActivity" />

        <activity android:name=".ui.winds.ui.ChangeLogActivity" />

    </application>

</manifest>
